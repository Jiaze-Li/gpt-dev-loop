{
  "name": "gpt-dev-loop",
  "version": "1.0.0",
  "private": true,
  "description": "SuperGPT V1: local autonomous coding workflow with role-routed providers and deterministic delivery.",
  "type": "module",
  "bin": {
    "supergpt": "bin/supergpt.js",
    "supergpt-mcp": "bin/supergpt-mcp.js"
  },
  "scripts": {
    "doctor": "node ./scripts/doctor.js",
    "test": "node --test tests/*.test.js",
    "install-global": "node ./bin/install-plugin.js",
    "uninstall-global": "node ./bin/install-plugin.js --uninstall",
    "benchmark": "node ./bin/benchmark-tokens.js",
    "benchmark:live": "node ./bin/benchmark-tokens.js --live",
    "probe:provider-overhead": "node ./bin/probe-provider-overhead.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  }
}
